# Set the boot menu timeout to zero seconds.
set timeout=0
# Set the default menu entry index.
set default=0
# Begin the menu entry block.
menuentry "vibeos" {
# Load the kernel using the multiboot2 command.
    multiboot2 /boot/kernel.elf
# Boot the loaded kernel.
    boot
# End the menu entry block.
}
