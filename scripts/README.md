# Scripts

Build and run scripts will live here. We'll add them in M0.

## Toolchain
- `scripts/toolchain.sh`: builds an `x86_64-elf` cross-compiler (binutils + gcc).
- Set `AUTO_INSTALL_DEPS=1` to install Ubuntu build dependencies via `apt-get`.
